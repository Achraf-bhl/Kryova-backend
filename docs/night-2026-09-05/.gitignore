# Screenshots are evidence for one session, not source. The report names what each
# one showed; the bytes are large, they are regenerated by re-running, and a repo
# already carrying 450 MB of third-party PDFs does not need more binary history.
*.png
