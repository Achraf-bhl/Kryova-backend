# Direct runtime dependencies. Transitive versions are resolved by pip.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

SQLAlchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4
# database.py also builds an async engine (async_engine / AsyncSessionLocal /
# get_async_db in app/core/database.py) that needs its own driver -- psycopg's
# sync driver above doesn't cover the async engine.
asyncpg==0.31.0

pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0
python-dotenv==1.2.3

python-jose[cryptography]==3.5.0
bcrypt==5.0.0
# Declared directly, not left as a transitive of python-jose: `core/security.py`
# imports AESGCM and HKDF itself to seal TOTP secrets at rest (P1.7). An import
# that no requirements file names is how `ezdxf` became a landmine here -- it was
# present on the machine that wrote it and absent everywhere else.
cryptography==50.0.0
redis>=5.0,<9.0

# Meshing and FEA
numpy==2.5.2
scipy==1.18.0
gmsh==4.15.2

# The OCCT geometry kernel (app/kernel/) -- the primary compilation target for the
# design IR, so geometry can be built headless, free, in CI and deterministically.
# `cadquery-ocp` and not `pythonocc-core`: the latter is not published on PyPI at all
# and would drag conda into the deployment (measured 2026-09-05, master plan Phase 1.0).
#
# It is BIG: ~166 MB for OCP itself plus ~640 MB of VTK pulled in as a hard dependency
# and never used by this codebase -- nothing here renders through VTK. Stripping it is
# a container-layer job (master plan P9.2), not something to fake with --no-deps here.
#
# `app/kernel/` imports and the whole test suite runs without it -- same contract the
# CATIA bridge keeps for pywin32 -- so an install that omits it degrades to the CATIA
# backend rather than breaking.
cadquery-ocp==7.9.3.1.1

# Local vector indexes
faiss-cpu==1.15.0

# Reading the reference manuals (app/retrieval/). Pure Python, so it installs
# anywhere with no system package -- which is the point: `pdftotext` from
# poppler is preferred when present because it is far faster and better on
# multi-column pages, but it cannot be assumed, and an install that cannot read
# a PDF silently indexes nothing.
pypdf==6.4.0

# Reading attachments (app/documents/, master plan P4.2). Both MIT.
# `ezdxf` also writes DXF (app/manufacture/dxf.py). It was imported by both
# paths and named in no requirements file until 2026-09-14, so neither could
# run anywhere built from this file, and tests/test_documents_readers.py --
# which importorskips it at module level -- skipped every test it holds,
# the PDF and plain-text ones included.
# `openpyxl` reads .xlsx. Word, PowerPoint, CSV and HTML need no package: they
# are read with the standard library (app/documents/office.py says why).
ezdxf==1.4.4
openpyxl==3.1.5

# CATIA V5 COM bridge (app/catia/). The marker keeps Linux/macOS installs
# working -- the bridge imports everywhere and refuses at call time off Windows.
pywin32==312; sys_platform == "win32"

# AI providers talk HTTP. The Anthropic SDK is deliberately NOT here: the
# default provider is local Ollama, and only AI_PROVIDER=anthropic needs it.
# Install it with `pip install anthropic` if you pick that provider.
httpx==0.28.1

# Federated engineering libraries (Decision 2: integrate, never re-implement).
# Both Apache-2.0 and pure Python apart from pyLife's pandas/h5py wheels. Until
# 2026-09-14 pyproject.toml called these "real dependencies" while no
# requirements file named either, so neither was installed anywhere and every
# test that needed one skipped itself — the ezdxf shape again, one layer up.
# OpenMDAO (E10.3) is the second optimisation driver behind app/optimise's seam;
# pyLife (E8) is the fatigue engine app/fatigue/backend.py federates to.
openmdao==3.45.1
pylife==2.3.1

# Laya (ConvAI Innovations, Apache-2.0) -- a 421M non-autoregressive decision
# model, the open-weight shape TypeSafe's Jev introduced (System One Models,
# 2026-09-15): typed Choice/Score/Noul questions over a state, answered in one
# forward pass with a calibrated probability per option, never a token of text.
# `app/ai/laya_decide.py` is the one caller: routing "which tool family does
# this request need" off the agent's own slow provider and onto a model small
# enough to answer in milliseconds on hardware the 9B generator does not fit.
# torch is the CPU wheel by default; a CUDA build is a per-machine install
# (`pip install torch --index-url https://download.pytorch.org/whl/cu121`),
# never pinned here because the right CUDA/driver pairing is per-GPU, not
# per-checkout.
laya==0.3.11
torch==2.5.1
